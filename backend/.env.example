# Optional: enables Whisper transcription and GPT queue explanations
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
WHISPER_MODEL=whisper-1
DATABASE_URL=sqlite:///./opd_smartqueue.db

# Keep false for the hackathon demo unless you have configured credentials.
USE_LLM=false
